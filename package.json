{
  "name": "no-more-bs",
  "version": "0.5.1",
  "description": "Bootstrap shared behavioural instructions and skills across Claude Code, Codex, OpenCode, Pi, and GitHub Copilot.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/1stNox/no-more-bs.git"
  },
  "module": "src/index.ts",
  "type": "module",
  "private": false,
  "bin": {
    "no-more-bs": "./dist/index.js"
  },
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "build": "bun build src/index.ts --target=node --outfile=dist/index.js",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "prepack": "bun run scripts/validate-frontmatter.ts && bun run build",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@types/bun": "latest",
    "lefthook": "^2.1.6"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.4.2",
    "diff": "^9.0.0"
  }
}
